using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This enum is used to determine whether to remove a card from a list of cards.
public enum RemoveOption
{
    Remove,
    DontRemove
}
