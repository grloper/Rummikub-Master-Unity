using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This enum is used to determine where to add a card in a list of cards.
public enum AddPosition
{
    Beginning, //0
    Middle, //1
    End //2
}
